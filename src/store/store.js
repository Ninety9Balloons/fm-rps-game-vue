import { ref } from "vue";

// Store for player's choice
export const playerPick = ref();

// Store for player's choice
export const computerPick = ref();

// Store for the score
export const score = ref(0);

// Store for the winner
export const winner = ref();

// Store for rules modal
export const rulesModal = ref(false);
